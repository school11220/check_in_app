# Viva and interview questions

1. Walk through registration to paid ticket.
2. Why is payment signature verification server-side?
3. Why does the event counter update use a conditional transaction?
4. What happens if a payment verification request is repeated?
5. How does `hasEventAccess()` differ for Admin and Organizer?
6. Why is middleware not enough for API security?
7. What does the ticket token protect?
8. What makes a timed QR expire?
9. What is the audit checksum used for?
10. What happens when SMTP is unavailable?
11. Why are prices represented as integers?
12. How are promo discounts distributed across tickets?
13. Which data is in Clerk and which is in PostgreSQL?
14. Why does the schema use JSON for schedules?
15. What tests exist for the pricing algorithm?
16. What security issue follows from missing Upstash variables?
17. What is the largest scalability risk?
18. How would you add a session API?
19. Is there an AI feature? Prove the answer from the repository.
20. Why is `prisma db push` weaker than migrations for production?
