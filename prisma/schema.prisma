generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_PRISMA_URL")
}

model AuditLog {
  id         String   @id
  action     String
  resource   String
  resourceId String?
  details    Json?
  userId     String
  userName   String
  userRole   String
  ipAddress  String?
  userAgent  String?
  createdAt  DateTime @default(now())

  @@index([action])
  @@index([createdAt])
  @@index([resource])
}

model User {
  id               String   @id // Clerk user ID
  email            String   @unique
  firstName        String?
  lastName         String?
  name             String?
  password         String?
  imageUrl         String?
  role             String   @default("UNAUTHORIZED")
  assignedEventIds String[] @default([])
  isActive         Boolean  @default(true)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  organizedEvents  Event[]  @relation("OrganizerEvents")
  tickets          Ticket[]

  @@index([role])
}

model Event {
  id                   String        @id
  name                 String
  description          String?
  date                 DateTime
  startTime            String        @default("09:00")
  endTime              String        @default("18:00")
  venue                String?
  address              String?
  price                Int           @default(0)
  entryFee             Int           @default(0)
  prizePool            Int           @default(0)
  category             String        @default("other")
  imageUrl             String?
  capacity             Int           @default(100)
  soldCount            Int           @default(0)
  isActive             Boolean       @default(true)
  isFeatured           Boolean       @default(false)
  schedule             Json          @default("[]")
  speakers             Json          @default("[]")
  sponsors             Json          @default("[]")
  tags                 String[]      @default([])
  organizer            String?
  organizerId          String?
  contactEmail         String?
  contactPhone         String?
  termsAndConditions   String?
  registrationDeadline String?
  earlyBirdEnabled     Boolean       @default(false)
  earlyBirdPrice       Int           @default(0)
  earlyBirdDeadline    String?
  sendReminders        Boolean       @default(false)
  createdAt            DateTime      @default(now())
  updatedAt            DateTime
  registrationFields   Json          @default("[]")
  organizerVideoLink   String?
  videoLink            String?
  gallery              Json          @default("[]")
  PricingRule          PricingRule[]
  Review               Review[]
  Session              Session[]
  Ticket               Ticket[]
  CertificateTemplate  CertificateTemplate?
  OrganizerUser        User?         @relation("OrganizerEvents", fields: [organizerId], references: [id], onDelete: SetNull)

  @@index([category])
  @@index([isActive])
  @@index([organizerId])
}

model Integration {
  id        String   @id
  provider  String   @unique
  name      String
  type      String
  isEnabled Boolean  @default(false)
  config    Json     @default("{}")
  createdAt DateTime @default(now())
  updatedAt DateTime
}

model PricingRule {
  id              String   @id
  eventId         String
  triggerType     String
  triggerValue    Int
  adjustmentType  String
  adjustmentValue Int
  active          Boolean  @default(true)
  createdAt       DateTime @default(now())
  updatedAt       DateTime
  Event           Event    @relation(fields: [eventId], references: [id], onDelete: Cascade)

  @@index([active])
  @@index([eventId])
}

model Review {
  id        String   @id
  eventId   String
  userName  String
  rating    Int
  comment   String
  createdAt DateTime @default(now())
  Event     Event    @relation(fields: [eventId], references: [id], onDelete: Cascade)

  @@index([eventId])
}

model Session {
  id              String   @id
  eventId         String
  title           String
  description     String?
  type            String   @default("talk")
  speakerName     String?
  speakerRole     String?
  startTime       String
  endTime         String
  date            String
  capacity        Int      @default(100)
  registeredCount Int      @default(0)
  createdAt       DateTime @default(now())
  updatedAt       DateTime
  Event           Event    @relation(fields: [eventId], references: [id], onDelete: Cascade)

  @@index([eventId])
}

model SiteConfig {
  id        String   @id @default("default")
  settings  Json     @default("{}")
  templates Json     @default("[]")
  createdAt DateTime @default(now())
  updatedAt DateTime
  surveys   Json     @default("[]")
}

model Ticket {
  id                String       @id
  name              String
  email             String?
  phone             String?
  eventId           String
  status            String       @default("pending")
  token             String?
  razorpayOrderId   String?
  razorpayPaymentId String?
  amountPaid        Int          @default(0)
  grossAmount       Int          @default(0)
  discountAmount    Int          @default(0)
  refundedAmount    Int          @default(0)
  paymentMethod     String?
  checkedIn         Boolean      @default(false)
  checkedInAt       DateTime?
  checkedInBy       String?
  certificateSent   Boolean      @default(false)
  purchaseGroupId   String?
  cancelledAt       DateTime?
  lastDeliveredAt   DateTime?
  deliveryCount     Int          @default(0)
  createdAt         DateTime     @default(now())
  updatedAt         DateTime
  customAnswers     Json         @default("{}")
  promoCodeId       String?
  userId            String?
  Event             Event        @relation(fields: [eventId], references: [id], onDelete: Cascade)
  User              User?        @relation(fields: [userId], references: [id], onDelete: SetNull)
  CheckInLogs       CheckInLog[]
  DeliveryLogs      TicketDeliveryLog[]

  @@index([email])
  @@index([eventId])
  @@index([razorpayOrderId])
  @@index([status])
  @@index([token])
  @@index([eventId, status])
  @@index([userId])
  @@index([paymentMethod])
}

model CheckInLog {
  id            String   @id @default(uuid())
  ticketId      String
  eventId       String
  action        String   // "checkin" | "undo_checkin" | "manual_checkin"
  performedBy   String   // userId or scanner name
  performedRole String   // "ADMIN" | "ORGANIZER" | "SCANNER"
  ipAddress     String?
  userAgent     String?
  deviceId      String?  // for offline sync identification
  reason        String?
  checksum      String   // HMAC of ticketId+action+timestamp for tamper evidence
  syncedAt      DateTime? // null if created online, set when offline record syncs
  createdAt     DateTime @default(now())
  ticket        Ticket   @relation(fields: [ticketId], references: [id], onDelete: Cascade)

  @@index([ticketId])
  @@index([eventId])
  @@index([createdAt])
  @@index([performedBy])
}

model TicketDeliveryLog {
  id          String   @id @default(uuid())
  ticketId    String
  channel     String
  recipient   String?
  success     Boolean
  error       String?
  requestedBy String?
  createdAt   DateTime @default(now())
  ticket      Ticket   @relation(fields: [ticketId], references: [id], onDelete: Cascade)

  @@index([ticketId])
  @@index([channel])
  @@index([createdAt])
}

model SecurityEvent {
  id        String   @id @default(uuid())
  type      String
  key       String
  ticketId  String?
  eventId   String?
  ipAddress String?
  userAgent String?
  details   Json?
  createdAt DateTime @default(now())

  @@index([type])
  @@index([key])
  @@index([ticketId])
  @@index([eventId])
  @@index([createdAt])
}

model PromoCodeRecord {
  id              String    @id @default(uuid())
  code            String
  discountType    String    // "percentage" | "fixed"
  discountValue   Int       // percentage (0-100) or fixed amount in paise
  maxUses         Int       @default(100)
  usedCount       Int       @default(0)
  maxUsesPerUser  Int       @default(1)
  minTickets      Int       @default(1)
  stackable       Boolean   @default(false)
  eventIds        String[]  @default([])
  expiresAt       DateTime
  startsAt        DateTime  @default(now())
  isActive        Boolean   @default(true)
  createdBy       String?
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt

  @@unique([code])
  @@index([code])
  @@index([isActive])
  @@index([expiresAt])
}

model PromoUsage {
  id          String   @id @default(uuid())
  promoCode   String
  ticketId    String
  userId      String?  // email or clerk userId
  eventId     String
  discount    Int      // discount amount applied in paise
  createdAt   DateTime @default(now())

  @@index([promoCode])
  @@index([userId])
  @@index([eventId])
}



model CertificateTemplate {
  id              String   @id @default(cuid())
  name            String
  eventId         String?  @unique
  Event           Event?   @relation(fields: [eventId], references: [id])
  bgImage         String?  // URL to background image
  elements        Json?    // JSON array of elements { type: 'text'|'image', x: 0, y: 0, ... }
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
